#include<stdio.h>
#include<string.h>
void main()
{
    char str[10];
    gets(str);
    int l=strlen(str);
    int arr[l+1];
    int i;
    for(i=1;i<l+1;i++)
    {
        arr[i]=0;
    }
    for(i=1;i<=math.pow(2,l);i++)
    {
      for(j=l;j>0;j--)
      {
          for(k=l;k>0;k--)
          {

          }
      }
    }

}
